<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.personaje">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Personaje"
        tools:targetApi="31">


        <activity
            android:name=".Inicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".DatosPersonaje"
            tools:ignore="WrongManifestParent">
        </activity>

        <activity android:name=".PantallaDado">

        </activity>

        <activity android:name=".CiudadActivity">

        </activity>

        <activity android:name=".ObjetoActivity">

        </activity>

        <activity android:name=".EnemigoActivity">

        </activity>



    </application>

</manifest>